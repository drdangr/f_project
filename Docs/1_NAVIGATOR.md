# 🗺️ Навігатор покращень – З чого почати?

## Для новачка: де знайти потрібну інформацію?

### 📚 Хочу швидко розібратися в проєкті (5 хвилин)
👉 Відкрийте **README.md** → розділ **"🚀 Для нових розробників"**

Там ви знайдете:
- Порядок читання файлів
- Ключові патерни
- Приклади коду

---

### 🔍 Не розумію, що робить функція `parse_input()`
👉 Відкрийте **cli.py** → функція `parse_input()` (рядки 15-41)

Там ви знайдете:
- Приклади розбору команд
- Пояснення, що таке `shlex.split()`
- Приклади з лапками

```python
'add "John Doe" 1234567890' → cmd='add', args=['John Doe', '1234567890']
```

---

### 🎯 Не розумію, що таке `@input_error` та `@mutating`
👉 Відкрийте **commands.py** → рядки 77-144

**`@input_error`** (рядки 77-112):
- Перехоплює KeyError, ValueError, IndexError
- Виводить дружні повідомлення про помилки
- Приклад використання

**`@mutating`** (рядки 114-144):
- Автоматично зберігає на диск
- Не зберігає під час помилки або сигналу `__EXIT__`
- Приклади

---

### 📞 Не розумію регулярний вираз для телефону
👉 Відкрийте **config.py** → рядки 10-13

```python
# Шаблон для телефону: ^ (початок) + рівно 10 цифр + $ (кінець)
# Приклади: '1234567890' ✓, '123' ✗, '12345678901' ✗
PHONE_REGEX = r"^\d{10}$"
```

---

### 📅 Неочевидна логіка розрахунку днів до дня народження
👉 Відкрийте **models.py** → методи класу `Record`

**`days_to_birthday()`** (рядки 180-206):
- Покроковий алгоритм (1, 2, 3, 4 кроки)
- Практичні приклади
- Коментарі прямо в коді

**`get_next_birthday()`** (рядки 208-229):
- Аналогічний алгоритм, але повертає дату

---

### ➕ Хочу додати нову команду
👉 Відкрийте **README.md** → розділ "🚀 Для нових розробників" → "Як додати нову команду?"

Знайдете приклад за 30 секунд:
```python
@REG.register("new-cmd", help='...')
@input_error
@mutating
def cmd_new_cmd(args, storage):
    return "Результат"
```

---

### 🧪 Хочу протестувати код
👉 Відкрийте **README.md** → розділ "🚀 Для нових розробників" → "Як протестувати свій код?"

Приклади тестування:
- Валідація одного поля
- Тест команди
- Повний прогін



## 📂 Структура документації

```
f_project/
├── README.md (головний файл)
│   └─ Розділ "🚀 Для нових розробників"
├── 1_NAVIGATOR.md (швидкий навігатор по проєкту)
├── 2_SUMMARY.md (коротке саммарі проєкту)
├── 3_ARCHITECTURE.md (опис архітектури проєкту)
├── 4_COMMANDS.md (детальний розбір найскладнішого модуля — обробника команд)
│
└── Джерельний код із коментарями:
    ├── config.py (regex пояснений)
    ├── cli.py (parse_input() задокументована)
    ├── commands.py (@input_error, @mutating описані)
    └── models.py (Record та логіка днів народження пояснені)
```



## ❓ Часті запитання

**Q: З чого почати?**  
A: Читайте у порядку: README → config.py → storage.py → models.py → commands.py.  
   За потреби відкривайте документи з папки `Docs` у тій послідовності, у якій вони пронумеровані.

**Q: Де знайти приклад нової команди?**  
A: У README.md, розділ "Як додати нову команду?"

**Q: Незрозумілий regex у config.py.**  
A: Відкрийте config.py — там є приклади валідних і невалідних значень.

**Q: Як працює `@input_error`?**  
A: У commands.py є docstring із прикладами.

**Q: Як обчислюються дні до дня народження?**  
A: Дивіться `models.py`, метод `days_to_birthday()` з покроковим поясненням.
