# 👤 Персональний помічник (CLI)

> Простий, модульний та розширюваний персональний помічник з інтерфейсом командного рядка.
>
> ## 🚀 Для нових розробників

### Як швидко розібратися у проєкті (за 5 хвилин)

**Порядок читання файлів коду:**
1. **config.py** (32 рядків) — усі константи в одному місці
2. **storage.py** (51 рядків) — як зберігаються дані
3. **models.py** (388 рядків) — класи для контактів та нотаток
4. **commands.py** (673 рядків) — кожна команда = одна функція
5. **cli.py** (202 рядків) — парсить введення та викликає команди
6. **main.py** (18 рядків) — запускає CLI
7. **color_helper.py** (93 рядків) - для використання колорами, бейджів та іконок
8. **.flake8** (5 рядків) - для зняття обмеження довжини рядка в 79 символів
8. **/Docs** (6 файлів) - повна документация проекту

**Ключові патерни проєкту:**

```python
# 1. Валідація — під час створення об'єкта (не після)
phone = Phone("1234567890")  # ✓ Добре
phone = Phone("123")  # ✗ ValueError одразу

# 2. Помилки через винятки (не if/else)
try:
    rec = storage.contacts.get_record("Alice")
except KeyError:
    print("Not found")

# 3. Декоратори для наскрізної функціональності
@REG.register("cmd-name", help="Usage: cmd-name arg", min_args=1)
@input_error      # Обробка помилок
@mutating         # Автозбереження
def cmd_example(args, storage):
    return "Результат"

# 4. Команда автоматично у help
result = REG.help_text()
```

### Як додати нову команду?

1. Оберіть розділ (`SECTION_PHONEBOOK`, `SECTION_NOTES`, `SECTION_SYSTEM` або `"Прочее"` для нетипових команд). Детальніше про групи команд — у `Docs/4_COMMANDS.md`.
2. Додайте саму функцію в `commands.py` та зареєструйте її через `@REG.register`.

```python
# У commands.py (і більше ніде!)

@REG.register(
    "new-cmd",
    help='Usage: new-cmd "arg1" arg2',
    section=SECTION_PHONEBOOK,  # оберіть відповідний розділ
    min_args=2,  # мінімальна кількість аргументів (валідується автоматично)
)
@input_error
@mutating  # Якщо команда змінює дані
def cmd_new_cmd(args: List[str], storage: Storage) -> str:
    # Валідація аргументів відбувається автоматично до виклику функції
    # Ваша логіка тут
    arg1 = args[0]
    arg2 = args[1]
    return "Результат"
```

### Як протестувати свій код?

```bash
# Тест одного поля
python3 -c "from models import Phone; Phone('1234567890'); print('OK')"

# Тест команди безпосередньо
python3 -c "
from commands import cmd_help
from storage import Storage
storage = Storage()
print(cmd_help([], storage))
"

# Повний тест (виконати кілька команд)
python3 -c "
from models import Record, Name, Phone
from storage import Storage, save_storage

storage = Storage()
rec = Record(Name('Test'))
rec.add_phone(Phone('1111111111'))
storage.contacts.add_record(rec)
save_storage(storage)
print('✓ Contact created and saved')
"
```

### Структура коду: як це працює?

```
Користувач вводить: add "John" 1234567890
              ↓
      parse_input() парсить ввод
              ↓
    ["add"] + ["John", "1234567890"]
              ↓
   REG.resolve("add") шукає функцію
              ↓
    cmd_add(["John", "1234567890"], storage)
              ↓
  @input_error перехоплює помилки
              ↓
  @mutating автоматично зберігає результат
              ↓
    Виводить: "Added contact John with phone..."
```

### Де що знаходиться?

| Треба                          | Знайти                                                     |
| ------------------------------ | ---------------------------------------------------------- |
| Додати валідацію для поля      | `models.py` → клас `Field` або підкласи                    |
| Нова команда                   | `commands.py` → функція `cmd_*`                            |
| Змінити формат збереження      | `storage.py` → функції `save_storage()` / `load_storage()` |
| Змінити константу              | `config.py` → будь-яке значення з UPPERCASE                |
| Зрозуміти, як парситься команда| `cli.py` → функція `parse_input()`                         |


## Функціональність

### Управління контактами
- ✅ Додавання контактів з назвою
- ✅ Додавання/редагування/видалення телефонів (10 цифр)
- ✅ Додавання/видалення email-адрес
- ✅ Встановлення адреси
- ✅ Встановлення дня народження (DD.MM.YYYY)
- ✅ Пошук за всіма полями
- ✅ Показ контактів із пагінацією
- ✅ Список днів народження на найближчий тиждень

### Записна книжка (Нотатки)
- ✅ Додавання текстових нотаток
- ✅ Додавання/видалення тегів до нотаток
- ✅ Редагування тексту нотатки
- ✅ Видалення нотаток
- ✅ Пошук за текстом та назвою
- ✅ Пошук за тегами
- ✅ Сортування за назвою або датою створення

### Розумна функціональність
- ✅ Сувора відповідність команд
- ✅ Дружні повідомлення про помилки
- ✅ Підтримка лапок для аргументів із пробілами

### Збереження
- ✅ Автоматичне збереження після кожної операції
- ✅ Збереження у домашній папці (~/.personal_assistant_cli/)
- ✅ Безпечне завантаження з обробкою помилок

## Модульна архітектура

```
┌─────────────────────────────────────┐
│        main.py (точка входу)        │
└───────────────┬─────────────────────┘
                │
                ↓
     ┌───────────────────────┐
     │   cli.py (інтерфейс)  │
     │  parse_input, run_cli │
     │    run_cli()          │
     └──────────┬────────────┘
                │
      ┌─────────┴─────────┐
      ↓                   ↓
┌──────────────┐ ┌──────────────────┐
│  commands.py │ │  storage.py      │
│  (45+ cmds)  │ │ (load/save data) │
└──────┬───────┘ └────────┬─────────┘
       │                  │
       └────────┬─────────┘
                ↓
       ┌──────────────────┐
       │   models.py      │
       │ (Record, Note)   │
       └────────┬─────────┘
                │
                ↓
      ┌──────────────────┐
      │   config.py      │
      │  (константи)     │
      └──────────────────┘
```

## Запуск

### Вимоги
- Python 3.9+
- Залежності з `requirements.txt` (наприклад, `prompt-toolkit`)

### Старт
```bash
python.exe -m pip install -r requirements.txt # Встановити залежності
python3 main.py
```

### Приклад сеансу
```
personal_assistant_cli v1.0.0. Type 'help' for commands.
Data stored in: /Users/username/.personal_assistant_cli/storage.pkl

> add "John Doe" 1234567890
Added contact John Doe with phone 1234567890.

> add "John Doe" 0987654321
Phone added for John Doe.

> phone "John Doe"
John Doe: 1234567890, 0987654321

> add-birthday "John Doe" 15.03.1990
Birthday set for John Doe.

> show-birthday "John Doe"
John Doe: 15.03.1990

> birthdays
Today:
  John Doe — 15.03.1990 (Friday)

> hello
Привіт! Чим можу допомогти?

> add-note "Project Ideas" "1. REST API\n2. Add tests"
Note added: Project Ideas

> tag-add "Project Ideas" work urgent
Tags added to 'Project Ideas': urgent, work

> find-tag "work"
Project Ideas [#urgent #work]
1. REST API
2. Add tests

> exit
Bye!
```

## Команди

### Контакти

| Команда                          | Опис                                           |
|----------------------------------|------------------------------------------------|
| `add "Name" [0123456789]`        | Додати новий контакт [або телефон до існуючого]|
| `change "Name" old10 new10`      | Змінити телефонний номер контакту              |
| `phone "Name"`                   | Показати всі телефони контакту                 |
| `all`                            | Вивести всі контакти адресної книги            |
| `add-birthday "Name" DD.MM.YYYY` | Додати дату народження                         |
| `show-birthday "Name"`           | Показати дату народження                       |
| `birthdays`                      | Показати дні народження на найближчий тиждень  |

**Додаткові команди для контактів**

| Команда      | Опис                                                  |
|--------------------------------------|-------------------------------|
| `find query`                         | Пошук за всіма полями контакту|
| `delete-contact "Name"`              | Видалити контакт              |
| `add-email "Name" user@mail.com`     | Додати email                  |
| `remove-email "Name" user@mail.com`  | Видалити email                |
| `set-address "Name" "Kyiv, Ukraine"` | Встановити адресу             |

### Нотатки

| Команда                         | Опис                                    |
|---------------------------------|-----------------------------------------|
| `add-note "Title" текст...`     | Додати нотатку (увесь текст після назви)|
| `list-notes [title\|created]`   | Показати нотатки                        |
| `find-note query`               | Пошук за текстом                        |
| `find-tag tag`                  | Пошук за тегом                          |
| `edit-note "Title" новий_текст` | Редагувати текст (усе після назви)      |
| `tag-add "Title" tag1 tag2`     | Додати теги                             |
| `tag-remove "Title" tag`        | Видалити тег                            |
| `delete-note "Title"`           | Видалити нотаток                        |

### Система

| Команда          | Опис                     |
|------------------|--------------------------|
| `hello`          | Привітання від помічника |
| `help`           | Показати довідку         |
| `version`        | Версія програми          |
| `close` / `exit` | Вихід з програми         |

## Структура файлів

```
f_project/
├── config.py              — Константи (667B)
├── models.py              — Моделі даних (11K)
├── storage.py             — Збереження (1.4K)
├── commands.py            — Команди (14K)
├── cli.py                 — Інтерфейс (2.8K)
├── main.py                — Точка входу (530B)
├── README.md              — Цей файл
└── Docs/
    ├── 0_TZ.md                  — Завдання
    ├── 1_NAVIGATOR.md           — Навігатор по проєкту
    ├── 2_SUMMARY.md             — Короткий підсумок
    ├── 3_ARCHITECTURE.md        — Детальна архітектура
    ├── 4_COMMANDS.md            — Детальна документація по найскладнішому модулю
    └── TESTING.md               — Тестування
```



## Архітектура

### Раціональний дизайн

**Розділення відповідальності:**
- `config.py` — центр конфігурації (одне місце для змін)
- `models.py` — логіка даних (можна тестувати окремо)
- `storage.py` — персистентність
- `commands.py` — бізнес-логіка (команди)
- `cli.py` — користувацький інтерфейс
- `main.py` — орхестрація

**Переваги такого підходу:**
- ✅ Легко розширювати (нова команда = 5 рядків)
- ✅ Легко тестувати (кожна частина окремо)
- ✅ Легко мігрувати (на JSON, БД, web)
- ✅ Легко скорочувати помилки (централізована обробка)
- ✅ Читабельний код (кожен файл ~ 200 рядків)

### Потік даних

```
Вхід користувача
    ↓
parse_input()
    ↓
resolve(command)
    ↓
handler(args, storage)
    ↓
validate → modify → save
    ↓
return result
```


## Тестування

Ознайомтеся з `TESTING.md` для прикладів:
- Валідація полів
- Управління контактами
- Робота з нотатками
- Обробка помилок

Запуск простого тесту:
```python
python3 -c "from models import Phone; print(Phone('1234567890'))"
# Вихід: 1234567890
```

## Розширення

Додавання нової команди за 3 кроки:

```python
# 1. У commands.py
from models import SomeModel

# 2. Реєструємо з мінімальною кількістю аргументів
@REG.register("your-cmd", help="Usage: your-cmd arg", min_args=1)
@input_error
def cmd_your(args: List[str], storage: Storage) -> str:
    # Валідація відбувається автоматично
    # Логіка
    return "Результат"

# 3. Готово! Автоматично у help
```

## Валідація

Усі дані валідуються на вході:

```
Телефон:  ^[0-9]{10}$     (рівно 10 цифр)
Email:    RFC-lite        (user@domain.extension)
День РД:  DD.MM.YYYY      (формат дати)
Ім'я:     будь-який рядок
Адреса:   будь-який рядок
```

## Збереження

- Формат: pickle (бінарний, швидкий)
- Місце: `~/.personal_assistant_cli/storage.pkl`
- Автозбереження: після кожної операції
- Відновлення: у разі пошкодження автоматично створюється новий файл

## Введення команд

- Команди вводяться англійською і відповідають переліку з розділу «Контакти» / «Система».
- Аргументи (імена, адреси, тексти нотаток) можуть бути будь-якою мовою та містити пробіли в лапках.
- Будь-яка помилка синтаксису або невідома команда повертає дружнє повідомлення від CLI.
