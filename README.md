# 👤 Персональний помічник (CLI)

> Простий, модульний та розширюваний персональний помічник з інтерфейсом командного рядка.

## Функціональність

### Управління контактами
- ✅ Додавання контактів з назвою
- ✅ Додавання/редагування/видалення телефонів (10 цифр)
- ✅ Додавання/видалення email адрес
- ✅ Встановлення адреси
- ✅ Встановлення дня народження (DD.MM.YYYY)
- ✅ Пошук по всім полям
- ✅ Показ контактів з пагінацією
- ✅ Список днів народження на наступний тиждень

### Записна книжка (Нотатки)
- ✅ Додавання текстових нотаток
- ✅ Додавання/видалення тегів до нотаток
- ✅ Редагування текста нотатки
- ✅ Видалення нотаток
- ✅ Пошук по текстові та назві
- ✅ Пошук по тегам
- ✅ Сортування по назві або даті створення

### Умна функціональність
- ✅ Строге відповідність команд (без алиасів і підказок)
- ✅ Дружні повідомлення про помилки
- ✅ Підтримка кавичок для аргументів з пробілами

### Збереження
- ✅ Автоматичне збереження після кожної операції
- ✅ Збереження у домашній папці (~/.personal_assistant_cli/)
- ✅ Безпечне завантаження з обробкою помилок

## Модульна архітектура

```
┌─────────────────────────────────────┐
│        main.py (точка входу)        │
└──────────────┬──────────────────────┘
               │
               ↓
     ┌─────────────────────┐
    │    cli.py (UI)      │
    │  parse, run_cli,    │
    │    run_cli()        │
     └────────┬────────────┘
              │
      ┌───────┴────────┐
      ↓                ↓
┌──────────────┐ ┌──────────────────┐
│  commands.py │ │  storage.py      │
│  (45+ cmds)  │ │ (load/save data) │
└──────┬───────┘ └────────┬─────────┘
       │                  │
       └──────────┬───────┘
                  ↓
           ┌────────────────┐
           │   models.py    │
           │ (Record, Note) │
           └────────┬───────┘
                    │
                    ↓
          ┌──────────────────┐
          │   config.py      │
          │ (константи)      │
          └──────────────────┘
```

## Запуск

### Вимоги
- Python 3.10+
- Тільки стандартна бібліотека (без залежностей!)

### Старт
```bash
python3 main.py
```

### Приклад сеансу
```
personal_assistant_cli v1.0.0. Type 'help' for commands.
Data stored in: /Users/username/.personal_assistant_cli/storage.pkl

> add "John Doe" 1234567890
Added contact John Doe with phone 1234567890.

> add "John Doe" 0987654321
Phone added for John Doe.

> phone "John Doe"
John Doe: 1234567890, 0987654321

> add-birthday "John Doe" 15.03.1990
Birthday set for John Doe.

> show-birthday "John Doe"
John Doe: 15.03.1990

> birthdays
Today:
  John Doe — 15.03.1990 (Friday)

> hello
Привіт! Чим можу допомогти?

> add-note "Project Ideas" "1. REST API\n2. Add tests"
Note added: Project Ideas

> tag-add "Project Ideas" work urgent
Tags added to 'Project Ideas': urgent, work

> find-tag "work"
Project Ideas [#urgent #work]
1. REST API
2. Add tests

> exit
Bye!
```

## Команди

### Контакти

| Команда                          | Опис                                           |
|----------------------------------|------------------------------------------------|
| `add "Name" [0123456789]`        | Додати новий контакт [або телефон до існуючого]|
| `change "Name" old10 new10`      | Змінити телефонний номер контакту              |
| `phone "Name"`                   | Показати всі телефони контакту                 |
| `all`                            | Вивести всі контакти адресної книги            |
| `add-birthday "Name" DD.MM.YYYY` | Додати дату народження                         |
| `show-birthday "Name"`           | Показати дату народження                       |
| `birthdays`                      | Показати дні народження на найближчий тиждень  |

**Додаткові команди для контактів**

| Команда      | Опис                                                 |
|--------------------------------------|------------------------------|
| `find query`                         | Пошук по всіх полях контакту |
| `delete-contact "Name"`              | Видалити контакт             |
| `add-email "Name" user@mail.com`     | Додати email                 |
| `remove-email "Name" user@mail.com`  | Видалити email               |
| `set-address "Name" "Kyiv, Ukraine"` | Встановити адресу            |

### Нотатки

| Команда                       | Опис                                    |
|-------------------------------|-----------------------------------------|
| `add-note "Title" текст...`   | Додати заметку (весь текст після назви) |
| `list-notes [title\|created]` | Показати заметки                        |
| `find-note query`             | Пошук по тексту                         |
| `find-tag tag`                | Пошук по тегу                           |
| `edit-note "Title" новий_текст` | Редагувати текст ( текст після назви) |
| `tag-add "Title" tag1 tag2`   | Додати теги                             |
| `tag-remove "Title" tag`      | Видалити тег                            |
| `delete-note "Title"`         | Видалити заметку                        |

### Система

| Команда          | Опис                     |
|------------------|--------------------------|
| `hello`          | Привітання від помічника |
| `help`           | Показати довідку         |
| `version`        | Версія програми          |
| `close` / `exit` | Вихід з програми         |

## Структура файлів

```
f_project/
├── config.py              — Константи (667B)
├── models.py              — Моделі даних (11K)
├── storage.py             — Збереження (1.4K)
├── commands.py            — Команди (14K)
├── cli.py                 — Інтерфейс (2.8K)
├── main.py                — Точка входу (530B)
└── Docs/
    ├── TZ.md              — Завдання
    ├── README.md              — Цей файл
    ├── ARCHITECTURE.md        — Докладна архітектура
    └── TESTING.md             — Тестування
```

**Всього:** ~42K коду (розділено на 5 логічних модулів)

## Архітектура

### Раціональний дизайн

**Розділення відповідальності:**
- `config.py` — центр конфігурації (1 місце для змін)
- `models.py` — логіка даних (можна тестувати окремо)
- `storage.py` — персистентність
- `commands.py` — бізнес-логіка (команди)
- `cli.py` — користувацький інтерфейс
- `main.py` — орхестрація

**Переваги такого підходу:**
- ✅ Легко розширювати (нова команда = 5 рядків)
- ✅ Легко тестувати (кожна частина окремо)
- ✅ Легко міграти (на JSON, БД, web)
- ✅ Легко скорочувати помилки (централізована обробка)
- ✅ Читаємо код (кожен файл ~ 200 рядків)

### Потік даних

```
Вхід користувача
    ↓
parse_input()
    ↓
resolve(command)
    ↓
handler(args, storage)
    ↓
validate → modify → save
    ↓
return result
```

## Тестування

Бачити `TESTING.md` для прикладів:
- Валідація полів
- Управління контактами
- Роботи з заметками
- Обробка помилок

Запуск простого тесту:
```python
python3 -c "from models import Phone; print(Phone('1234567890'))"
# Вихід: 1234567890
```

## Розширення

Додавання нової команди за 3 кроки:

```python
# 1. У commands.py
from models import SomeModel

# 2. Реєструємо
@REG.register("your-cmd", help="Опис")
@input_error
def cmd_your(args: List[str], storage: Storage) -> str:
    # Логіка
    return "Результат"

# 3. Готово! Автоматично у help
```

## Валідація

Всі дані валідуються на вході:

```
Телефон:  ^[0-9]{10}$     (рівно 10 цифр)
Email:    RFC-lite        (user@domain.extension)
День РД:  DD.MM.YYYY      (формат дати)
Ім'я:     будь-яка строка
Адреса:   будь-яка строка
```

## Збереження

- Формат: pickle (бінарний, швидкий)
- Місце: `~/.personal_assistant_cli/storage.pkl`
- Автозбереження: після кожної операції
- Восстановление: при損ировании автоматично створює новий файл

## Ввід команд

- Команди вводяться строго англійською і відповідають переліку з розділу «Контакти» / «Система».
- Аргументи (імена, адреси, тексти нотаток) можуть бути будь-якою мовою та містити пробіли в лапках.
- Будь-яка помилка синтаксису або невідома команда повертає дружнє повідомлення від CLI.
